language: node_js

node_js:
- "lts/*"

os:
- linux
- osx

cache: npm

branches:
  only:
  - master

before_install:
  - npm i -g npm@latest

install:
<<<<<<< HEAD
- npm install && cd client && npm install && cd ..
- npm install && cd server && npm install && cd ..
=======
- > 
  if [["$TRAVIS_BRANCH" === /^front/]]; then 
    npm install;
    cd client;
    npm install;
    cd ..
- if ["$TRAVIS_BRANCH" === /^back/]; then npm install && cd back && npm install && cd ..
>>>>>>> Test

script:
- npm run lint
- npm run style

notifications:
  email:
    on_success: never
    on_failure: always